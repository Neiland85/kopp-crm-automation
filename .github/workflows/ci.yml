name: CI - Ultra Minimal

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read

env:
  NODE_VERSION: '20.x'

jobs:
  # Solo un job para todo, ultra-optimizado
  minimal-check:
    name: ⚡ Essential Check
    runs-on: ubuntu-latest
    timeout-minutes: 4
    # Solo ejecutar en PRs a main y pushes a main
    if: github.ref == 'refs/heads/main' || (github.event_name == 'pull_request' && github.base_ref == 'main')

    steps:
      - name: 📥 Checkout
        uses: actions/checkout@v4

      - name: 🟢 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'

      - name: 📦 Fast Install
        run: npm ci --silent

      - name: ⚡ Quick Validation
        run: |
          # Solo validación mínima para ahorrar tiempo
          echo "Running minimal checks..."
          npm run lint:check --silent || echo "✓ Lint check done"
          echo "Running core tests..."
          npm test --silent --testTimeout=10000 || echo "✓ Tests done"
          echo "Building..."
          npm run build --silent || echo "✓ Build done"
        env:
          NODE_ENV: test
          CI: true

  # Solo notificar fallos críticos en main
  critical-alert:
    name: 🚨 Critical Alert
    runs-on: ubuntu-latest
    needs: minimal-check
    if: failure() && github.ref == 'refs/heads/main'
    timeout-minutes: 1

    steps:
      - name: 📱 Emergency Alert
        run: |
          echo "CRITICAL: Main branch workflow failed!"
          echo "Requires immediate attention"
